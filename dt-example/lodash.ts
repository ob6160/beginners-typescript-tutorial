import shuffle from "lodash/shuffle.js";

// Docs: https://lodash.com/docs/4.17.15#shuffle

console.log(shuffle(2)); // Returns an empty array.. note no type error when entering a number.

// ... Now we install the correct typings!
